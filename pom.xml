<?xml version="1.0" encoding="UTF-8"?>

<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="
           http://maven.apache.org/POM/4.0.0
           https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.jenkins-ci.plugins</groupId>
        <artifactId>plugin</artifactId>
        <version>6.2221.va_045130417c9</version>
        <relativePath/>
    </parent>

    <groupId>io.github.nauoss</groupId>
    <artifactId>jenkins-build-evidence-exporter</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>hpi</packaging>

    <name>Jenkins Build Evidence Exporter</name>

    <description>
        A Jenkins plugin that collects build metadata and exports it as portable JSON evidence.
    </description>

    <url>
        https://github.com/NAU-OSS/jenkins-build-evidence-exporter
    </url>

    <licenses>
        <license>
            <name>MIT License</name>
            <url>https://opensource.org/licenses/MIT</url>
        </license>
    </licenses>

    <properties>
        <!-- Minimum Jenkins version supported by this plugin -->
        <jenkins.version>2.555.3</jenkins.version>

        <!-- Java version used to build the plugin -->
        <java.version>25</java.version>
    </properties>

</project>